header.l-header
  a.l-header__logo-mask(href=basePath)
    img(src=`${basePath}/img/logo/logo.svg`, alt=siteName)

  //- input#nav-switcher(type="checkbox" hidden)

  //- label(for="nav-switcher" class="o-burger")
  //-   div(class="o-burger__line")
  //-   div(class="o-burger__line")
  //-   div(class="o-burger__line")

  button(class="o-burger" onclick="toggleNav()")
    div(class="o-burger__line")
    div(class="o-burger__line")
    div(class="o-burger__line")

  //- .c-nav 單純用來控制開合
  nav(class="c-nav l-header__nav" data-lenis-prevent)
    div(class="c-nav__menu")
      ul(class="c-nav__btn-box")
        li(class="c-nav__btn-wrap")
          a(href=`${basePath}/#lineup` class="c-nav__btn")
            span(class="u-font-en o-text-lg") LINEUP
            span 演出陣容
        li(class="c-nav__btn-wrap")
          a(href=`${basePath}/news` class="c-nav__btn")
            span(class="u-font-en o-text-lg") NEWS
            span 最新消息
        li(class="c-nav__btn-wrap")
          a(href=`${basePath}/#about` class="c-nav__btn")
            span(class="u-font-en o-text-lg") ABOUT
            span 關於大港
        li(class="c-nav__btn-wrap")
          a(href="##" class="c-nav__btn")
            span(class="u-font-en o-text-lg") TIMETABLE
            span 節目表
        li(class="c-nav__btn-wrap")
          a(href="##" class="c-nav__btn")
            span(class="u-font-en o-text-lg") TICKET
            span 購票資訊
        li(class="c-nav__btn-wrap")
          a(href=`${basePath}/contact.html` class="c-nav__btn")
            span(class="u-font-en o-text-lg") MAP
            span 地圖導覽

      ul(class="c-nav__btn-box -icon")
        li(class="c-nav__btn-wrap")
          a(
            href="https://www.facebook.com/megaportfest/"
            target="_blank" class="c-nav__btn"
          )
            iconify-icon(icon="mingcute:facebook-fill" class="u-icon-md")
        li(class="c-nav__btn-wrap")
          a(
            href="https://www.instagram.com/megaportfest/"
            target="_blank" class="c-nav__btn"
          )
            iconify-icon(icon="mingcute:ins-fill" class="u-icon-md")
        li(class="c-nav__btn-wrap")
          a(
            href="https://www.youtube.com/channel/UC7xWU14_GGYKVqoSLg1M8EQ/feed"
            target="_blank" class="c-nav__btn"
          )
            iconify-icon(icon="mingcute:youtube-fill" class="u-icon-md")