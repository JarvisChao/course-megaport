meta(charset="UTF-8")
//- meta(name="viewport", content="width=device-width, initial-scale=1.0")

//- 新增 maximum-scale=1.0 防止手機點擊表單畫面放大
meta(name="viewport", content="width=device-width, initial-scale=1.0, maximum-scale=1.0")

//- 教學用網站，因此設置此 meta 防止瀏覽器索引
meta(name="robots" content="noindex, nofollow")

//- 防止 Iphone 自訂偵測電話，影響樣式
meta(name="format-detection" content="telephone=no")
meta(http-equiv="x-rim-auto-match" content="none")

//- FB 縮圖資訊，偵錯及預覽工具 https://developers.facebook.com/tools/debug/
meta(property="og:type", content="website")
meta(property="og:url", content=`${baseUrl}${basePath}${path}`)
meta(property="og:image", content=`${baseUrl}${basePath}${image}`)
meta(roperty="og:description", content=description)
meta(name="description" content=description)

//- <Page Title> - <Site Name>｜<Info Title>
//- js 字串模板使用反引號 `` 加 $ 字號 `${}`
- const newPageTitle = `${pageTitle ? `${pageTitle} - ` : ''}`;
- let title = `${newPageTitle}${siteName}｜${infoTitle}`
if !infoTitle
  - title = `${newPageTitle}${siteName}`
meta(property="og:title", content=title)
//- pug 的內容語法，使用 # 字號 #{}
title #{title}

//- 網頁 icon
link(rel="apple-touch-icon", sizes="180x180", href=`${basePath}/favicon/apple-touch-icon.png`)
link(rel="icon", type="image/png", sizes="32x32", href=`${basePath}/favicon/favicon-32x32.png`)
link(rel="icon", type="image/png", sizes="16x16", href=`${basePath}/favicon/favicon-16x16.png`)
link(rel="shortcut icon", href=`${basePath}/favicon/favicon.ico`)
link(rel="manifest", href=`${basePath}/favicon/site.webmanifest`)
link(rel="mask-icon", href=`${basePath}/favicon/safari-pinned-tab.svg", color="#ef8322`)
meta(name="msapplication-TileColor", content="#ef8322")
meta(name="theme-color", content="#ffffff")

script(src="https://cdn.jsdelivr.net/npm/iconify-icon@1.0.8/dist/iconify-icon.min.js")

//- 引入連結
//- css 引入也有順序，通常插件在自定義的 style 上面
link(rel="preconnect", href="https://fonts.googleapis.com")
link(rel="preconnect", href="https://fonts.gstatic.com", crossorigin)
link(href="https://fonts.googleapis.com/css2?family=Noto+Sans+TC:wght@400;500;900&family=Roboto:wght@700;900&display=swap", rel="stylesheet")

link(href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous")
link(rel="stylesheet" href="https://unpkg.com/lenis@1.1.18/dist/lenis.css")

//- 因為編譯完成 index.html 會在 dist 裡面
//- 所以要連動 css 要以 dist 資料夾為根目錄去思考
link(rel="stylesheet" href=`${basePath}/css/aos.css`)
link(rel="stylesheet" href=`${basePath}/css/swiper-bundle.min.css`)
link(rel="stylesheet" href=`${basePath}/css/style.css`)
